DB_NAME=financial_journal
DB_USER=postgres
DB_PASSWORD=changeme

MONOBANK_TOKEN=
MONOBANK_ACCOUNT_ID=

# Only matters if something ever calls the API from a different origin than
# the deployed frontend itself (same-origin requests through nginx don't need it).
CORS_ALLOWED_ORIGINS=http://your-server-ip-or-domain
